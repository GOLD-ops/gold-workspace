{
  "name": "gold-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Gold Workspace 个人工具站（Vue 前端 + Express 后端）",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "npm --prefix client run build",
    "start": "node server/index.js"
  }
}
